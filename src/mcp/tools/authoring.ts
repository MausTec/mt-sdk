/**
 * Authoring tools: scaffolding, examples, and guided plugin creation.
 *
 * Planned tools:
 *   - mtp_scaffold_plugin       : Generate a complete MTP plugin skeleton
 *   - mtp_list_example_plugins  : List available reference plugins
 *   - mtp_get_example_plugin    : Return full source of a reference plugin
 */

import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerAuthoringTools(_server: McpServer): void {
  // Tool registrations will go here.
}
