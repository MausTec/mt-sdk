/**
 * Language tools: MTP syntax documentation and reference.
 *
 * Planned tools:
 *   - mtp_get_language_reference  : Scoped syntax docs by topic
 *   - mtp_explain_json_action     : JSON action → plain English + MTP equivalent
 */

import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerLanguageTools(_server: McpServer): void {
  // Tool registrations will go here.
}
