#!/usr/bin/env node

/**
 * MCP server binary — launched by VSCode extension or standalone.
 *
 * Usage:
 *   mt-sdk-mcp              # stdio transport (default, for VSCode)
 */

import "../mcp/stdio.js";
